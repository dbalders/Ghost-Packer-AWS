sudo: required
dist: trusty

notifications:
  email:
    on_success: never
    on_failure: dbalders@gmail.com

addons:
  apt:
    sources:
      - chef-current-trusty
    packages:
      - chefdk

install: bundle install

services: docker

env:
  matrix:
  - INSTANCE=ubuntu-1604